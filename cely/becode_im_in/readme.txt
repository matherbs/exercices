This App work with nodejs

If you want to run it: 
first install nodejs on your machine
then go to this path on terminal and 
run following commande to install required modules:

	npm install 

That gonna install all nodejs modules defined on ./package.json

After install run the following command on same path to run app:

	node index

Check the terminal return to know on wich address app is running. 
